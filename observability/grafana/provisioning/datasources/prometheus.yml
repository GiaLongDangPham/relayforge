apiVersion: 1

datasources:
  - name: RelayForge Prometheus
    uid: relayforge-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
